import React from 'react';

export const FloatingBackToIndex: React.FC = () => {
  // Cleanly disabled to prevent interlocking with bottom-left LiveActivityTicker and bottom navigation
  return null;
};
